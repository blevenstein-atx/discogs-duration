# macOS
.DS_Store

# Secrets — never commit the real config
config.js

# Generated fresh at every deploy — never commit the real one
build-info.js

# Node (only relevant once/if you add tooling in v2)
node_modules/
npm-debug.log*

# Editor
.vscode/
.postman/
